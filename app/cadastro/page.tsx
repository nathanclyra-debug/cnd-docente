"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Cadastro(){const[r,setR]=useState({name:"",pin:"",confirm:""});const[err,setErr]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setErr("");if(!/^\d{4}$/.test(r.pin)||r.pin!==r.confirm){setErr("O PIN deve ter exatamente 4 números e as confirmações devem coincidir.");return}const x=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:r.name,pin:r.pin})});const j=await x.json();if(!x.ok){setErr(j.error);return}router.push("/documentos")}return <main className="wrap" style={{maxWidth:650}}><div className="card"><h1>Cadastro docente</h1><p className="muted">Informe seu nome completo e crie seu PIN numérico de 4 dígitos.</p><form onSubmit={submit}><label className="label">Nome completo</label><input className="input" value={r.name} onChange={e=>setR({...r,name:e.target.value})} required/><label className="label">PIN</label><input className="input" inputMode="numeric" maxLength={4} value={r.pin} onChange={e=>setR({...r,pin:e.target.value.replace(/\D/g,"").slice(0,4)})} required/><label className="label">Confirmar PIN</label><input className="input" inputMode="numeric" maxLength={4} value={r.confirm} onChange={e=>setR({...r,confirm:e.target.value.replace(/\D/g,"").slice(0,4)})} required/>{err&&<p className="danger status">{err}</p>}<div style={{marginTop:20}}><button className="btn">Criar cadastro</button></div></form></div></main>}