import {redirect} from "next/navigation";import {getSession} from "@/lib/server";import Link from "next/link";
export default async function Admin(){const s=await getSession();if(!s||s.role!=="admin")redirect("/login");return <main className="wrap"><div className="card"><div className="actions" style={{justifyContent:"space-between"}}><div><span className="status">ADMINISTRAÇÃO</span><h1>Painel administrativo</h1><p className="muted">Central de gestão da Carteira Nacional do Docente.</p></div></div><div className="actions" style={{marginTop:18}}><Link className="btn" href="/admin/candidatos">Candidatos e documentos</Link><Link className="btn" href="/admin/provas">Banco de provas</Link></div></div></main>}