import {redirect} from "next/navigation";import {db,getSession} from "@/lib/server";import Link from "next/link";
export default async function Admin(){const s=await getSession();if(!s||s.role!=="admin")redirect("/login");const{data:docs}=await db.from("documents").select("id,file_name,status,created_at,profile:profiles(name)").order("created_at",{ascending:false}).limit(100);return <main className="wrap"><div className="card"><div className="actions" style={{justifyContent:"space-between"}}><div><h1>Painel administrativo</h1><p className="muted">Gestão documental e habilitação.</p></div><Link className="btn" href="/admin/provas">Banco de provas</Link></div><table className="table"><thead><tr><th>Candidato</th><th>Documento</th><th>Status</th><th>Ação</th></tr></thead><tbody>{(docs??[]).map((d:any)=><tr key={d.id}><td>{d.profile?.name}</td><td>{d.file_name}</td><td><span className="status">{d.status}</span></td><td>{d.status==="pending"?<div className="actions"><form action="/api/admin/documents" method="post"><input type="hidden" name="id" value={d.id}/><input type="hidden" name="value" value="approved"/><button className="btn">Aprovar</button></form><form action="/api/admin/documents" method="post"><input type="hidden" name="id" value={d.id}/><input type="hidden" name="value" value="rejected"/><button className="btn red">Rejeitar</button></form></div>:"—"}</td></tr>)}</tbody></table></div></main>}