"use client";import {useState} from "react";export default function Consulta(){const[number,setNumber]=useState("");const[result,setResult]=useState<any>(null);const[error,setError]=useState("");async function verify(){setError("");setResult(null);const r=await fetch("/api/cnd/verify?number="+encodeURIComponent(number));const j=await r.json();if(!r.ok){setError(j.error||"CND não encontrada.");return}setResult(j)}return <main className="wrap"><div className="card"><span className="status">CONSULTA PÚBLICA</span><h1>Consultar CND</h1><p className="muted">Verifique a autenticidade e o status de uma Carteira Nacional do Docente.</p><div className="actions"><input className="input" placeholder="CND-2026-AB12CD34" value={number} onChange={e=>setNumber(e.target.value.toUpperCase())}/><button className="btn" onClick={verify}>Consultar</button></div>{error&&<p className="status danger" style={{marginTop:16}}>{error}</p>}{result&&<div className="question" style={{marginTop:18}}><span className="status ok">CND VÁLIDA</span><h2>{result.holder}</h2><p>Número: <b>{result.number}</b></p><p>Emissão: {new Date(result.issuedAt).toLocaleDateString("pt-BR")}</p><p>Status: <b>{result.status}</b></p></div>}</div></main>}