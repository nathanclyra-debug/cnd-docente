"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const[name,setName]=useState("");const[pin,setPin]=useState("");const[err,setErr]=useState("");const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();const x=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,pin})});const j=await x.json();if(!x.ok){setErr(j.error);return}router.push(j.role==="admin"?"/admin":"/dashboard")}return <main className="wrap" style={{maxWidth:520}}><div className="card"><h1>Acesso</h1><p className="muted">Entre com seu nome e PIN de 4 dígitos.</p><form onSubmit={go}><label className="label">Nome completo</label><input className="input" value={name} onChange={e=>setName(e.target.value)} required/><label className="label">PIN</label><input className="input" inputMode="numeric" maxLength={4} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,"").slice(0,4))} required/>{err&&<p className="danger status">{err}</p>}<div style={{marginTop:20}}><button className="btn">Entrar</button></div></form></div></main>}