import {db} from "@/lib/server";import {fail,json} from "@/lib/api";export async function GET(req:Request){const number=new URL(req.url).searchParams.get("number")?.trim();if(!number)return fail("Informe o número da CND.");const{data,error}=await db.from("cnd").select("number,issued_at,status,candidate:profiles(name)").eq("number",number).maybeSingle();if(error)return fail(error.message,500);if(!data)return fail("CND não encontrada.",404);return json({number:data.number,issuedAt:data.issued_at,status:data.status,holder:(data.candidate as any)?.name||"—"})}}