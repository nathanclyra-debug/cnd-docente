"use client";
import {useEffect,useMemo,useState} from "react";import {useRouter} from "next/navigation";
export default function ExamForm({examId,questions,timeLimitMinutes,attempted}:{examId:string;questions:any[];timeLimitMinutes:number|null;attempted:boolean}){const[a,setA]=useState<Record<string,string>>({});const[err,setErr]=useState("");const[busy,setBusy]=useState(false);const[seconds,setSeconds]=useState((timeLimitMinutes||0)*60);const router=useRouter();const answered=useMemo(()=>Object.keys(a).length,[a]);
useEffect(()=>{if(!timeLimitMinutes)return;const id=setInterval(()=>setSeconds(v=>{if(v<=1){clearInterval(id);if(!busy)send(true);return 0}return v-1}),1000);return()=>clearInterval(id)},[timeLimitMinutes,busy]);
async function send(auto=false){if(busy)return;setBusy(true);setErr("");const r=await fetch("/api/exams/submit",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({examId,answers:a})});const j=await r.json();if(!r.ok){setErr(j.error||"Não foi possível enviar.");setBusy(false);return}router.push("/resultado/"+j.attemptId)}
const mm=String(Math.floor(seconds/60)).padStart(2,"0"),ss=String(seconds%60).padStart(2,"0");
if(attempted)return <div className="card"><span className="status warn">TENTATIVA ENCERRADA</span><p>Você já atingiu o limite de tentativas deste exame.</p></div>;
return <div><div className={"examBar "+(seconds>0?"":"danger")}><div><b>Tempo restante</b><strong>{timeLimitMinutes?mm+":"+ss:"Sem limite"}</strong></div><div><b>Respondidas</b><strong>{answered}/{questions.length}</strong></div></div>{questions.map((q:any,i:number)=><div className="question" key={q.id}><div className="questionNumber">QUESTÃO {i+1}</div><h3>{q.statement}</h3>{q.image_url&&<img src={q.image_url} alt="" style={{maxWidth:"100%",maxHeight:420,objectFit:"contain",margin:"12px 0",borderRadius:12}}/>}<div>{(q.alternatives??[]).sort((x:any,y:any)=>x.order_index-y.order_index).map((o:any)=><label className={"option examOption "+(a[q.id]===o.id?"selected":"")} key={o.id}><input type="radio" name={q.id} value={o.id} checked={a[q.id]===o.id} onChange={e=>setA({...a,[q.id]:e.target.value})}/><span>{o.text}</span></label>)}</div></div>)}{err&&<p className="status danger">{err}</p>}<div className="examSubmit"><button className="btn" disabled={busy||seconds===0} onClick={()=>send(false)}>{busy?"Enviando...":"Finalizar exame"}</button><span className="muted">{answered} de {questions.length} questões respondidas</span></div></div>}