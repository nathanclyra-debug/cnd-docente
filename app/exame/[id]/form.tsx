"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function ExamForm({examId,questions}:{examId:string;questions:any[]}){const[a,setA]=useState<Record<string,string>>({});const[err,setErr]=useState("");const router=useRouter();async function send(){const r=await fetch("/api/exams/submit",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({examId,answers:a})});const j=await r.json();if(!r.ok){setErr(j.error);return}router.push("/resultado/"+j.attemptId)}return <div>{questions.map((q:any,i)=><div className="question" key={q.id}><b>{i+1}. {q.statement}</b>{q.image_url&&<img src={q.image_url} style={{maxWidth:"100%",marginTop:12}}/>}{(q.alternatives??[]).sort((x:any,y:any)=>x.order_index-y.order_index).map((o:any)=><label className="option" key={o.id}><input type="radio" name={q.id} value={o.id} onChange={e=>setA({...a,[q.id]:e.target.value})}/>{o.text}</label>)}</div>)}{err&&<p className="status danger">{err}</p>}<button className="btn" onClick={send}>Enviar exame</button></div>}