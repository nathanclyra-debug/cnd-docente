import {redirect,notFound} from "next/navigation";import {db,getSession} from "@/lib/server";import ExamForm from "./form";export default async function Exam({params}:{params:Promise<{id:string}>}){const s=await getSession();if(!s)redirect("/login");const{data:p}=await db.from("profiles").select("status").eq("id",s.id).single();if(p?.status!=="eligible")redirect("/exame");const{id}=await params;const{data:e}=await db.from("exams").select("*").eq("id",id).eq("status","open").single();if(!e)notFound();const{data:q}=await db.from("questions").select("id,statement,image_url,points,order_index,alternatives(id,text,order_index)").eq("exam_id",id).order("order_index");return <main className="wrap"><div className="card"><h1>{e.title}</h1><p className="muted">{e.description}</p><ExamForm examId={id} questions={q??[]}/></div></main>}